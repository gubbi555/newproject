pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                git branch: 'dev', url: 'https://github.com/gubbi555/newproject.git'
            }
        }
    }
}
